using System;
using ENode.Infrastructure;

namespace ENode.Domain
{
    /// <summary>Represents a provider to provide the aggregate root type and code mapping information.
    /// </summary>
    public interface IAggregateRootTypeCodeProvider : ITypeCodeProvider
    {
    }
}
