using System;
using ENode.Infrastructure;

namespace ENode.Eventing
{
    /// <summary>Represents a provider to provide the domain event handler type and code mapping information.
    /// </summary>
    public interface IEventHandlerTypeCodeProvider : ITypeCodeProvider
    {
    }
}
