using System;

namespace ENode.Eventing
{
    /// <summary>Represents a event which can be replayed when using event sourcing pattern.
    /// </summary>
    public interface ISourcableEvent
    {
    }
}
